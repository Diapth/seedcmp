# 澄清问题：IM Web UI 优化

**特性**：`001-im-web-ui-optimization`

**日期**：2026-06-03

**回答方式**：对于每个问题，在 `Answer:` 中填写选项字母、`yes/recommended` 表示接受推荐选项，或在允许的地方提供不超过 5 个字的简短自定义答案。

---

## Q1. 源参考审校深度

**推荐：A** - 这最能保护 UI 迁移：`manifest.pdf` 涵盖界面截图，`im_web` 承载成熟的行为逻辑，`uni` 承载当前的视觉原型。轻量级的记录式审校可减少实现偏差。

| 选项 | 说明 |
|--------|------|
| A | 实现前，审校 `im_web` 行为、`uni` 视觉原型和 `manifest.pdf`，然后在合约中记录映射笔记。 |
| B | 以 `manifest.pdf` 为主要基线；实现过程中参考 `im_web` 和 `uni`，但不单独做审校记录。 |
| C | 仅审校 P1 页面：登录页、主框架、会话列表、聊天详情和输入区。 |
| 简短回答 | 提供不同的简短答案（不超过 5 个字）。 |

**Answer:**
A

---

## Q2. 平台防护与依赖兼容性策略

**推荐：A** - 架构要求对 H5 和 APP-PLUS/Android 进行兼容性检查。在引入依赖前明确这一点，可防止隐藏的原生/H5 差异问题。

| 选项 | 说明 |
|--------|------|
| A | 每个新依赖和平台 API 都必须检查 H5 + APP-PLUS/Android 兼容性，并记录条件防护和回退说明。 |
| B | 仅 APP-PLUS/原生 API 需要显式防护；普通 npm 依赖仅在构建/测试时检查。 |
| C | 将兼容性检查推迟到 Android 验证发现具体问题之后。 |
| 简短回答 | 提供不同的简短答案（不超过 5 个字）。 |

**Answer:**
A

---

## Q3. 自动化 H5 冒烟/截图检查

**推荐：A** - 手动视觉 QA 是必需的，但在运行时搭建完成后添加少量自动化 H5 冒烟/截图检查，可为主要响应式断点提供可重复的覆盖面。

| 选项 | 说明 |
|--------|------|
| A | 运行时搭建后添加自动化 H5 冒烟/截图检查，同时在每个阶段保持手动视觉 QA 和问题关闭。 |
| B | 仅保留手动浏览器视觉 QA；首轮不添加自动化冒烟或截图检查。 |
| C | 仅在最终打磨阶段、所有 UI 页面实现后添加自动化冒烟检查。 |
| 简短回答 | 提供不同的简短答案（不超过 5 个字）。 |

**Answer:**
A
---

## Q4. 五秒识别成功准则

**推荐：A** - SC-003 已具备可衡量性；在每个阶段的 QA 中记录它，可防止"能快速理解界面"的目标沦为装饰性文字。

| 选项 | 说明 |
|--------|------|
| A | 在每个故事的视觉 QA 阶段验证五秒识别检查，并在 QA 问题日志中记录结果。 |
| B | 仅在最终打磨阶段验证五秒识别检查。 |
| C | 移除五秒准则，依赖通用视觉 QA。 |
| 简短回答 | 提供不同的简短答案（不超过 5 个字）。 |

**Answer:**
A
---

## Q5. 遗留模块推迟与二维码归属

**推荐：A** - 推迟的遗留模块需要可见的目标和理由。具体到群二维码，当前合约将其映射到群落地/详情面板，因此任务应对齐至此。

| 选项 | 说明 |
|--------|------|
| A | 推迟的遗留模块保留在覆盖合约中，包含目标路由/组件、理由和 QA 可见性；群二维码归属于 `pages/group/index.vue` 面板。 |
| B | 推迟的遗留模块仅保留在覆盖合约中；在开始实现前无需路由/组件占位符。 |
| C | 不推迟群二维码；在首个群管理轮次中实现它。 |
| 简短回答 | 提供不同的简短答案（不超过 5 个字）。 |

**Answer:**
B