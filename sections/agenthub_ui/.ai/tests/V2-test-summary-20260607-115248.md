# V2 测试执行摘要（2026-06-07 11:52:48）

目标地址：`http://172.18.58.156:5173/`

浏览器后端：本地 Node + Playwright Chromium。选择原因：需要真实 JS 页面、登录态、网络请求、storage 和截图证据；未接手任何人类已登录浏览器 session。

## 证据位置

- 测试截图与原始报告：`sections/agenthub_ui/.ai/tests/screenshots/v2-20260607-115248/`
- issue 截图：`sections/agenthub_ui/.ai/issue/screenshots/v2-20260607-115248/`
- 原始网络样本：`sections/agenthub_ui/.ai/tests/screenshots/v2-20260607-115248/requests-sample.json`
- diagnostics：`sections/agenthub_ui/.ai/tests/screenshots/v2-20260607-115248/diagnostics.log`
- 账号记录（token 已脱敏）：`sections/agenthub_ui/.ai/tests/screenshots/v2-20260607-115248/accounts.json`

## 校正后的结果

原始 `execution-report.md` 中有几处 `API 调用数=0`，原因是第一版 runner 只匹配了 `/auth`、`/users` 等路径，漏掉实际后端路径 `/v1/user/*`。以下结论以 `requests-sample.json` 与 `diagnostics.log` 为准。

| Case | 结论 | 说明 |
|---|---|---|
| V2-01-01 H5 启动可达 | Pass | 5173 可达，登录页可见，三档 viewport 截图已保存。 |
| V2-01-02 账号 A 登录 | Pass | 调用 `/v1/user/login`、`/v1/users/{uid}`、`/v1/users/{uid}/im`，写入真实 `auth.uid` / `auth.accessToken`。 |
| V2-01-03 token 持久化 | Pass | 刷新后仍在 `#/pages/chat/index`。 |
| V2-01-05 密码错误提示 | Pass | 错误密码调用 `/v1/user/login` 并返回 HTTP 400，页面停留登录页且展示错误。 |
| V2-02-01 进入注册页 | Fail | 缺少“确认密码”字段；与 V2 计划要求不符。 |
| V2-02-02 字段校验 | Fail | 7 位密码未按 `<8` 规则拦截，并实际触发注册成功。 |
| V2-02-03 验证码获取 | Pass | 调用 `/v1/user/sms/registercode`，出现倒计时。 |
| V2-02-04 B 注册成功 | Blocked / Fail | B 在 V2-02-02 中已被 7 位密码提前注册，标准密码 `Test1234!` 再注册返回 HTTP 400 duplicate。 |

## 发现的问题

- 已创建 issue：`sections/agenthub_ui/.ai/issue/V2-02.md`
- 重要副作用：账号 `13800000001` 已在验证步骤中被注册；本轮没有继续创建 C，避免在注册门禁不稳定时继续污染账号状态。
- 运行态与当前落盘源码存在不一致迹象：5173 服务出的 `pages/login/index.vue` 已使用 `useAuthStore`，而当前 workspace 的 `pages/login/index.vue` 仍含 `mock_token_abc123`。本轮结论以运行态页面和网络证据为准。
