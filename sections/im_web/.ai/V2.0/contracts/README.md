# Contracts

No new public external API contract is introduced by the planning phase.
The V2.0 work consumes existing IM backend endpoints and websocket behavior, so this directory is reserved for any story-level UI or flow contract notes that may be needed later.
